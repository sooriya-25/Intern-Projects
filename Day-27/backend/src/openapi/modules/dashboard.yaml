paths:
  /dashboard:
    get:
      tags: [Dashboard]
      summary: Get dashboard stats/summary for the current user
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Dashboard data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
